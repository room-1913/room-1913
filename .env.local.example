# Required: Anthropic API key (keep this secret, never commit real keys)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: override Anthropic API base URL (only set if using a proxy or
# regional endpoint; leave unset to use the default)
# ANTHROPIC_BASE_URL=https://api.anthropic.com
